# urthmapper
Vacant land prospect pipeline. Scrapes county GIS + OneMap, ranks parcels by seller motivation, serves an interactive map.

> The UI now requires real lead data and will not display generated demo leads. Add `clearwater-leads.json` or include `leads-data.js` with verified county leads before opening the app.
